import { createContext, useContext } from "react";
export const connectionFallback = {
    connection: undefined,
};
const ConnectionFallbackContext = createContext(connectionFallback);
export const useConnectionInternal = () => useContext(ConnectionFallbackContext);
//# sourceMappingURL=useConnectionOptionalFallback.js.map