/**
 * Handles the fact, that Wagmi Connectors do not have a "ready" property.
 *
 * @param connector
 */
export const isReady = (connector) => Object.prototype.hasOwnProperty.call(connector, "ready")
    ? connector.ready()
    : true;
//# sourceMappingURL=utils.js.map